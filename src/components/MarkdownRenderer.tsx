'use client';

// This component has been removed as the AI chat feature is no longer needed.
// You can delete this file.
export const MarkdownRenderer = ({ content }: { content: string }) => {
  return null;
};
